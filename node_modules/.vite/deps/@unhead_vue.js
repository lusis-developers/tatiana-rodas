import { C as ScriptNetworkEvents, a as useHead, b as callHook, c as useServerHead, d as VueResolver, f as headSymbol, i as injectHead, l as useServerHeadSafe, o as useHeadSafe, r as VueHeadMixin, s as useSeoMeta, t as createUnhead, u as useServerSeoMeta, y as walkResolver } from "./unhead.CfgPMHXt-j3GHCVRn.js";
import { Ft as onMounted, Kn as ref, Rn as isRef, Vn as onScopeDispose, gn as watch, ut as getCurrentInstance } from "./vue.runtime.esm-bundler-D9VUjP0e.js";
//#region node_modules/.pnpm/@unhead+vue@3.1.0_lightningcss@1.32.0_rolldown@1.0.0_typescript@6.0.3_vite@8.0.12_@type_278d362852e19ca1428813ec7fdc0caf/node_modules/@unhead/vue/dist/utils.mjs
function resolveUnrefHeadInput(input) {
	return walkResolver(input, VueResolver);
}
//#endregion
//#region node_modules/.pnpm/unhead@3.1.0_vite@8.0.12_@types+node@24.12.4_sass@1.99.0_yaml@2.9.0_/node_modules/unhead/dist/shared/unhead.DctB-0lW.mjs
function createNoopedRecordingProxy(instance = {}) {
	const stack = [];
	let stackIdx = -1;
	const handler = (reuseStack = false) => ({
		get(_, prop, receiver) {
			if (!reuseStack) {
				const v = Reflect.get(_, prop, receiver);
				if (typeof v !== "undefined") return v;
				stackIdx++;
				stack[stackIdx] = [];
			}
			stack[stackIdx].push({
				type: "get",
				key: prop
			});
			return new Proxy(() => {}, handler(true));
		},
		apply(_, __, args) {
			stack[stackIdx].push({
				type: "apply",
				key: "",
				args
			});
		}
	});
	return {
		proxy: new Proxy(instance || {}, handler()),
		stack
	};
}
function createForwardingProxy(target) {
	const handler = {
		get(_, prop, receiver) {
			const v = Reflect.get(_, prop, receiver);
			if (typeof v === "object") return new Proxy(v, handler);
			return v;
		},
		apply(_, __, args) {
			Reflect.apply(_, __, args);
		}
	};
	return new Proxy(target, handler);
}
function replayProxyRecordings(target, stack) {
	stack.forEach((recordings) => {
		let context = target;
		let prevContext = target;
		recordings.forEach(({ type, key, args }) => {
			if (type === "get") {
				prevContext = context;
				context = context[key];
			} else if (type === "apply") context = context.call(prevContext, ...args);
		});
	});
}
function resolveScriptKey(input) {
	return input.key || input.src || (typeof input.innerHTML === "string" ? input.innerHTML : "");
}
var PreconnectServerModes = ["preconnect", "dns-prefetch"];
function useScript$1(head, _input, _options) {
	const input = typeof _input === "string" ? { src: _input } : _input;
	const options = _options || {};
	const id = resolveScriptKey(input);
	const prevScript = head._scripts?.[id];
	if (prevScript) {
		prevScript.setupTriggerHandler(options.trigger);
		return prevScript;
	}
	options.beforeInit?.();
	const _events = [];
	const syncStatus = (s) => {
		script.status = s;
		_events.push({
			type: s,
			timestamp: Date.now()
		});
		callHook(head, "script:updated", hookCtx);
	};
	ScriptNetworkEvents.forEach((fn) => {
		const k = fn;
		const _fn = typeof input[k] === "function" ? input[k].bind(options.eventContext) : null;
		input[k] = (e) => {
			syncStatus(fn === "onload" ? "loaded" : fn === "onerror" ? "error" : "loading");
			_fn?.(e);
		};
	});
	const _cbs = {
		loaded: [],
		error: []
	};
	const _uniqueCbs = /* @__PURE__ */ new Set();
	const _registerCb = (key, cb, options2) => {
		if (head.ssr) return;
		if (options2?.key) {
			const key2 = `${options2?.key}:${options2.key}`;
			if (_uniqueCbs.has(key2)) return;
			_uniqueCbs.add(key2);
		}
		if (_cbs[key]) {
			const i = _cbs[key].push(cb);
			return () => _cbs[key]?.splice(i - 1, 1);
		}
		cb(script.instance);
		return () => {};
	};
	const loadPromise = new Promise((resolve) => {
		if (head.ssr) return;
		const emit = (api) => requestAnimationFrame(() => resolve(api));
		const unhook = head.hooks?.hook("script:updated", ({ script: script2 }) => {
			const status = script2.status;
			if (script2.id === id && (status === "loaded" || status === "error")) {
				if (status === "loaded") if (typeof options.use === "function") {
					const api = options.use();
					if (api) emit(api);
				} else emit({});
				else if (status === "error") resolve(false);
				unhook?.();
			}
		});
	});
	const script = {
		_loadPromise: loadPromise,
		_events,
		_warmupStrategy: void 0,
		instance: !head.ssr && options?.use?.() || null,
		proxy: null,
		id,
		src: input.src,
		input,
		status: "awaitingLoad",
		remove() {
			script._triggerAbortControllers?.forEach((ac) => ac.abort());
			script._triggerAbortControllers?.clear();
			script._triggerPromises = [];
			script._warmupEl?.dispose();
			if (script.entry) {
				script.entry.dispose();
				script.entry = void 0;
				syncStatus("removed");
				delete head._scripts?.[id];
				return true;
			}
			return false;
		},
		warmup(rel) {
			const { src } = input;
			const isCrossOrigin = !src.startsWith("/") || src.startsWith("//");
			const isPreconnect = rel && PreconnectServerModes.includes(rel);
			let href = src;
			if (!rel || isPreconnect && !isCrossOrigin) return;
			if (isPreconnect) {
				const $url = new URL(src);
				href = `${$url.protocol}//${$url.host}`;
			}
			const link = {
				href,
				rel,
				crossorigin: typeof input.crossorigin !== "undefined" ? input.crossorigin : isCrossOrigin ? "anonymous" : void 0,
				referrerpolicy: typeof input.referrerpolicy !== "undefined" ? input.referrerpolicy : isCrossOrigin ? "no-referrer" : void 0,
				fetchpriority: typeof input.fetchpriority !== "undefined" ? input.fetchpriority : "low",
				integrity: input.integrity,
				as: rel === "preload" ? "script" : void 0
			};
			script._warmupEl = head.push({ link: [link] }, {
				head,
				tagPriority: "high"
			});
			return script._warmupEl;
		},
		load(cb) {
			script._triggerAbortControllers?.forEach((ac) => ac.abort());
			script._triggerAbortControllers?.clear();
			script._triggerPromises = [];
			if (!script.entry) {
				syncStatus("loading");
				const defaults = {
					defer: true,
					fetchpriority: "low"
				};
				if (input.src && (input.src.startsWith("http") || input.src.startsWith("//"))) {
					defaults.crossorigin = "anonymous";
					defaults.referrerpolicy = "no-referrer";
				}
				script.entry = head.push({ script: [{
					...defaults,
					...input
				}] }, options);
			}
			if (cb) _registerCb("loaded", cb);
			return loadPromise;
		},
		onLoaded(cb, options2) {
			return _registerCb("loaded", cb, options2);
		},
		onError(cb, options2) {
			return _registerCb("error", cb, options2);
		},
		setupTriggerHandler(trigger) {
			if (script.status !== "awaitingLoad") return;
			if ((typeof trigger === "undefined" || trigger === "client") && !head.ssr || trigger === "server") script.load();
			else if (trigger instanceof Promise) {
				if (head.ssr) return;
				const abortController = new AbortController();
				script._triggerAbortControllers = script._triggerAbortControllers || /* @__PURE__ */ new Set();
				script._triggerAbortControllers.add(abortController);
				const abortPromise = new Promise((resolve) => {
					abortController.signal.addEventListener("abort", () => {
						script._triggerAbortControllers?.delete(abortController);
						resolve();
					});
				});
				script._triggerAbortController = abortController;
				script._triggerPromises = script._triggerPromises || [];
				const idx = script._triggerPromises.push(Promise.race([trigger.then((v) => typeof v === "undefined" || v ? script.load : void 0), abortPromise]).catch(() => {}).then((res) => {
					res?.();
				}).finally(() => {
					script._triggerPromises?.splice(idx, 1);
				}));
			} else if (typeof trigger === "function") trigger(script.load);
		},
		_cbs
	};
	loadPromise.then((api) => {
		if (api !== false) {
			script.instance = api;
			_cbs.loaded?.forEach((cb) => cb(api));
			_cbs.loaded = null;
		} else {
			_cbs.error?.forEach((cb) => cb());
			_cbs.error = null;
		}
	});
	const hookCtx = { script };
	script.setupTriggerHandler(options.trigger);
	if (options.use) {
		const { proxy, stack } = createNoopedRecordingProxy(head.ssr ? {} : options.use() || {});
		script.proxy = proxy;
		script.onLoaded((instance) => {
			replayProxyRecordings(instance, stack);
			script.proxy = createForwardingProxy(instance);
		});
	}
	if (!options.warmupStrategy && (typeof options.trigger === "undefined" || options.trigger === "client")) options.warmupStrategy = "preload";
	if (options.warmupStrategy) {
		script._warmupStrategy = options.warmupStrategy;
		script.warmup(options.warmupStrategy);
	}
	head._scripts = Object.assign(head._scripts || {}, { [id]: script });
	return script;
}
//#endregion
//#region node_modules/.pnpm/unhead@3.1.0_vite@8.0.12_@types+node@24.12.4_sass@1.99.0_yaml@2.9.0_/node_modules/unhead/dist/index.mjs
function defineLink(link) {
	return link;
}
function defineScript(script) {
	return script;
}
//#endregion
//#region node_modules/.pnpm/@unhead+vue@3.1.0_lightningcss@1.32.0_rolldown@1.0.0_typescript@6.0.3_vite@8.0.12_@type_278d362852e19ca1428813ec7fdc0caf/node_modules/@unhead/vue/dist/shared/vue.D__tEKct.mjs
function registerVueScopeHandlers(script, scope) {
	if (!scope) return;
	const _registerCb = (key, cb) => {
		if (!script._cbs[key]) {
			cb(script.instance);
			return () => {};
		}
		let i = script._cbs[key].push(cb);
		const destroy = () => {
			if (i) {
				script._cbs[key]?.splice(i - 1, 1);
				i = null;
			}
		};
		onScopeDispose(destroy);
		return destroy;
	};
	script.onLoaded = (cb) => _registerCb("loaded", cb);
	script.onError = (cb) => _registerCb("error", cb);
	const triggerAbortController = script._triggerAbortController;
	onScopeDispose(() => {
		triggerAbortController?.abort();
	});
}
function useScript(_input, _options) {
	const input = typeof _input === "string" ? { src: _input } : _input;
	const options = _options || {};
	const head = options?.head || injectHead();
	options.head = head;
	const scope = getCurrentInstance();
	options.eventContext = scope;
	if (scope && typeof options.trigger === "undefined") options.trigger = onMounted;
	else if (isRef(options.trigger) || typeof options.trigger === "function" && options.trigger.length === 0) {
		const trigger = options.trigger;
		let off;
		options.trigger = new Promise((resolve) => {
			off = watch(trigger, (val) => {
				if (val) resolve(true);
			}, { immediate: true });
			onScopeDispose(() => resolve(false), true);
		}).then((val) => {
			off?.();
			return val;
		});
	}
	head._scriptStatusWatcher = head._scriptStatusWatcher || head.hooks.hook("script:updated", ({ script: s }) => {
		if (s._statusRef) s._statusRef.value = s.status;
	});
	const script = useScript$1(head, input, options);
	script._statusRef = script._statusRef || ref(script.status);
	registerVueScopeHandlers(script, scope);
	return new Proxy(script, { get(_, key, a) {
		return Reflect.get(_, key === "status" ? "_statusRef" : key, a);
	} });
}
//#endregion
//#region node_modules/.pnpm/@unhead+vue@3.1.0_lightningcss@1.32.0_rolldown@1.0.0_typescript@6.0.3_vite@8.0.12_@type_278d362852e19ca1428813ec7fdc0caf/node_modules/@unhead/vue/dist/index.mjs
var unheadVueComposablesImports = { "@unhead/vue": [
	"injectHead",
	"useHead",
	"useSeoMeta",
	"useHeadSafe"
] };
//#endregion
export { VueHeadMixin, createUnhead, defineLink, defineScript, headSymbol, injectHead, resolveUnrefHeadInput, unheadVueComposablesImports, useHead, useHeadSafe, useScript, useSeoMeta, useServerHead, useServerHeadSafe, useServerSeoMeta };

//# sourceMappingURL=@unhead_vue.js.map